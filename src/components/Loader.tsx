import { Center, Loader as L } from "@mantine/core";

const Loader = () => {
    return (
        <Center style={{ width: "100%", height: "100vh" }}>
            <L />
        </Center>
    );
};

export default Loader;
